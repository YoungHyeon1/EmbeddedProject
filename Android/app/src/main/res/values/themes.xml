<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.App" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">#497136</item>
    </style>

    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        // Set the splash screen background, animated icon, and animation duration.
        <item name="windowSplashScreenBackground">#FFFFFF</item>

        // Use windowSplashScreenAnimatedIcon to add either a drawable or an
        // animated drawable. One of these is required.
        <item name="windowSplashScreenAnimatedIcon">@mipmap/splash_asset_test_round</item>
        <item name="windowSplashScreenAnimationDuration">200</item>  # Required for
        <item name="android:windowSplashScreenBrandingImage">@mipmap/splash_bt</item>
    # animated icons
        <item name="postSplashScreenTheme">@style/Theme.App</item>
    </style>
</resources>